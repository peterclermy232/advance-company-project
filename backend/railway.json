{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn advance_company.wsgi:application --bind 0.0.0.0:$PORT --workers 3"
  }
}